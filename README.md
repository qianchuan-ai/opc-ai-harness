# dsh-opc-recipes · 一人公司 dsh 插件策展库

> **开源下来不一定好用，我替你先踩坑。**

我是谦川（AI宇宙狂想家 · 枢台出品）。

这不是 dsh 壳、不是全量黄页、不是通用市场——而是**给「一人公司 / OPC 主理人」挑出来的、我亲测装通过、并记下真实坑的 dsh 插件清单**。你照着装，少走弯路。

---

## 为什么是现在（宏观趋势）

商业环境正在剧烈重构：**一人公司 / 小团队会越来越依赖 AI 生产力去扛原本需要三五个人才能做的活**。这不是赌某个工具的红火，而是押一条长期曲线——当人力贵、试错频、身兼多职成为常态，能把"本要自己磨但没空做的 grind"交给 AI 的人，才有弹性产能。

dsh（及同类"一切皆插件"的 harness）只是当前最顺手的载体。趋势没改，它就不过时；它哪天被更好的取代，策展的判断力仍在。**热点皆过客，道是归处**——本库策展的"道"，就是 OPC 主理人怎么用 AI 插件把产能撑起来。

---

## 它是什么 / 不是什么

| | 说明 |
|---|---|
| ✅ **是** | 按 OPC 主理人真实使用场景分类的 dsh 插件策展；每个都我亲测 `dsh plugin add` 装通过，附真实坑 |
| ❌ **不是** | 桌面壳、不是全量插件黄页、不是「我替你配好」的付费服务、不承诺「获客 / 变现」等结果 |

> 通用中文壳已有人免费做了。我做的是它不屑做的那层：**替你先踩安装坑，把「能装起来」变成可复制的步骤。**
> 工具不创造需求，也不保证生意——它只解决「已知要做什么之后，怎么更省力地做」。

---

## 已亲测插件（按功能）

| 插件 | 功能 | 对 OPC 主理人 | 批注 |
|---|---|---|---|
| [dsh-at-file](recipes/上下文与文件引用/dsh-at-file/) | `@` 引用文件 / 目录进上下文 | 丢文档给 agent，免复制粘贴 | ✅ 认 |
| [modlens](recipes/视觉与OCR/modlens/) | 图片 / OCR / 截图结构化解析 | 贴设计稿截图让 agent 读懂 | ✅ 认（⚠️ Win 有 bin 警告） |
| [dsh-turn-rewind](recipes/试错与回退/dsh-turn-rewind/) | 会话状态回退 | 改坏了能退，试错少成本 | ✅ 认 |
| [treg](recipes/研究工具目录/treg/) | 2600+ 外部接口目录（按事检索） | 研究向，需免费 token | ⚠️ 存疑（ingredient） |

> 全部于 2026-08-17 由小p 代跑 `dsh plugin --profile web add` 实装，`dsh plugin --profile web list` 复核在册。

---

## 通用坑（所有 `dsh plugin add` 都会撞，先读）

**Windows / WorkBuddy 环境专属**：`dsh plugin add` 底层转发给 pnpm，pnpm 的「安全删除」会调系统回收站。在 WorkBuddy 沙箱里该调用被拦截，报 `trash operation aborted` 直接挂掉。
→ **修复**：用空 `CODEBUDDY_SESSION_ID=` 跑安装命令，shim 即 early-return，pnpm 回归原生删除（删的都是它自己的临时暂存目录，安全）：

```bash
CODEBUDDY_SESSION_ID= npx -y @deepseek-ai/dsh plugin --profile web add <owner/repo>
```

（你本机若不走 WorkBuddy 终端，一般不会撞这个，照常装即可。）

其它常见坑：

- **缺 pnpm**：Developer Preview 文档没强调，但 `dsh plugin add` 依赖 pnpm。先 `npm i -g pnpm`。
- **GitHub HEAD 偶发 ECONNRESET / ETIMEDOUT**：网络抖动，pnpm 自动重试，不用管。
- **首装 compose 重**：首次拉全量 profile 依赖可能 1–3 分钟，别当卡死。

---

## 怎么用

1. 装好任一免费中文 dsh 环境（别人已解决，不重复造）；
2. 进对应插件目录，复制「导入命令」执行（注意上方通用坑）；
3. 崩了 → 看该插件的「真实坑」小节与 `谦川批注/`。

---

## 谦川批注体系

- ✅ **认** —— 实测好用，推荐
- ⚠️ **存疑** —— 部分场景可用 / 有门槛，注意限制
- ❌ **否** —— 不推荐 / 有坑
- 🕒 **待亲测** —— 仅情报占位，不背书

> 买手店不是黄页：我替你筛过、踩过坑、装通过，你才看不到垃圾。

---

## 版本与免责

dsh 仍是 **Developer Preview**，官方明说「未来将出现破坏兼容性的变更」。
所有插件标注**实测版本**；配置会随版本腐烂，崩了按批注找降级方案。

---

## 门面三张脸（归一个道：枢台 · AI效率搭子）

- **GitHub（你在这）** = 技术门面：插件真身 + 试用记录 + 批注
- **公众号 / 小红书 / 视频号** = 内容门面：引流
- **微信** = 私域：信任沉淀
