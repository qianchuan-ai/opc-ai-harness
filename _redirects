# ============================================================
# Cloudflare Pages 重定向规则（消除 soft-404）
# 静态资源（已存在的文件）优先于本文件处理，不受影响。
# 仅对「不存在的路径」生效。
#
# 1) 保留 SPA 路由：recipe-detail 由 index.html + 查询参数渲染
# 2) 其余未命中路径 → 返回 404 状态码 + 自定义 404.html
# ============================================================

/recipe-detail                 /index.html    200
/*                             /404.html      404